--[[
    Fryzer UI Library - DEBUG VERSION
    A modern UI library for Roblox
    Author: Fryzer
]]

print("🚀 Fryzer UI Library Loading...")

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

print("✅ Services loaded successfully")

local FryzerUI = {}
FryzerUI.__index = FryzerUI

-- Utility Functions
local function Tween(instance, properties, duration, style, direction)
    local tweenInfo = TweenInfo.new(
        duration or 0.3,
        style or Enum.EasingStyle.Quad,
        direction or Enum.EasingDirection.Out
    )
    local tween = TweenService:Create(instance, tweenInfo, properties)
    tween:Play()
    return tween
end

local function MakeDraggable(frame, dragFrame)
    local dragging, dragInput, dragStart, startPos
    
    local function update(input)
        local delta = input.Position - dragStart
        frame.Position = UDim2.new(
            startPos.X.Scale, 
            startPos.X.Offset + delta.X, 
            startPos.Y.Scale, 
            startPos.Y.Offset + delta.Y
        )
    end
    
    dragFrame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    
    dragFrame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            update(input)
        end
    end)
    
    print("✅ Made draggable:", dragFrame.Name)
end

-- Main Library Function
function FryzerUI:CreateWindow(config)
    print("📦 Creating Window...")
    
    local Window = {}
    Window.Tabs = {}
    Window.CurrentTab = nil
    
    -- Validate config
    config = config or {}
    config.Title = config.Title or "Fryzer Hub"
    config.Version = config.Version or "v1.0"
    config.Size = config.Size or UDim2.new(0, 500, 0, 350) -- Smaller default size
    
    print("⚙️ Config:", config.Title, config.Version)
    
    -- Wait for PlayerGui
    local success, playerGui = pcall(function()
        return LocalPlayer:WaitForChild("PlayerGui", 10)
    end)
    
    if not success or not playerGui then
        warn("❌ Failed to get PlayerGui!")
        return nil
    end
    
    print("✅ PlayerGui found")
    
    -- Create ScreenGui
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "FryzerGui"
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    ScreenGui.ResetOnSpawn = false
    ScreenGui.IgnoreGuiInset = true
    ScreenGui.DisplayOrder = 999
    
    -- Try to parent to CoreGui first (for better compatibility)
    local parentSuccess = pcall(function()
        ScreenGui.Parent = game:GetService("CoreGui")
        print("✅ Parented to CoreGui")
    end)
    
    if not parentSuccess then
        ScreenGui.Parent = playerGui
        print("✅ Parented to PlayerGui")
    end
    
    -- Main Background
    local MainBg = Instance.new("Frame")
    MainBg.Name = "MainBg"
    MainBg.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    MainBg.AnchorPoint = Vector2.new(0.5, 0.5)
    MainBg.Position = UDim2.new(0.5, 0, 0.5, 0)
    MainBg.Size = config.Size
    MainBg.ClipsDescendants = true
    MainBg.BorderSizePixel = 0
    MainBg.Active = true
    MainBg.Parent = ScreenGui
    
    print("✅ Main background created")
    
    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 29)
    MainCorner.Parent = MainBg
    
    -- Top Bar
    local TopBar = Instance.new("Frame")
    TopBar.Name = "TopBar"
    TopBar.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    TopBar.Size = UDim2.new(1, 0, 0, 62)
    TopBar.BorderSizePixel = 0
    TopBar.Parent = MainBg
    
    local TopBarCorner = Instance.new("UICorner")
    TopBarCorner.CornerRadius = UDim.new(0.2, 8)
    TopBarCorner.Parent = TopBar
    
    local TopBarExtend = Instance.new("Frame")
    TopBarExtend.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    TopBarExtend.Position = UDim2.new(0, 0, 0.8, 0)
    TopBarExtend.Size = UDim2.new(1, 0, 0, 11)
    TopBarExtend.BorderSizePixel = 0
    TopBarExtend.Parent = TopBar
    
    print("✅ Top bar created")
    
    -- Title
    local Title = Instance.new("TextLabel")
    Title.Name = "Title"
    Title.BackgroundTransparency = 1
    Title.Position = UDim2.new(0.01, 0, 0, 0)
    Title.Size = UDim2.new(0, 200, 0, 62)
    Title.Font = Enum.Font.GothamBold
    Title.Text = config.Title
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextSize = 18
    Title.TextXAlignment = Enum.TextXAlignment.Left
    Title.Parent = TopBar
    
    -- Version Tag
    local VersionTag = Instance.new("Frame")
    VersionTag.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
    VersionTag.Position = UDim2.new(0.695, 0, 0.13, 0)
    VersionTag.Size = UDim2.new(0, 101, 0, 40)
    VersionTag.BorderSizePixel = 0
    VersionTag.Parent = TopBar
    
    local VersionCorner = Instance.new("UICorner")
    VersionCorner.CornerRadius = UDim.new(0, 55)
    VersionCorner.Parent = VersionTag
    
    local VersionText = Instance.new("TextLabel")
    VersionText.BackgroundTransparency = 1
    VersionText.Size = UDim2.new(1, 0, 1, 0)
    VersionText.Font = Enum.Font.GothamBold
    VersionText.Text = config.Version
    VersionText.TextColor3 = Color3.fromRGB(255, 255, 255)
    VersionText.TextSize = 16
    VersionText.Parent = VersionTag
    
    -- Close Button
    local CloseButton = Instance.new("TextButton")
    CloseButton.BackgroundTransparency = 1
    CloseButton.Position = UDim2.new(0.918, 0, 0.117, 0)
    CloseButton.Size = UDim2.new(0, 50, 0, 43)
    CloseButton.Font = Enum.Font.GothamBold
    CloseButton.Text = "X"
    CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    CloseButton.TextSize = 20
    CloseButton.BorderSizePixel = 0
    CloseButton.Parent = TopBar
    
    CloseButton.MouseButton1Click:Connect(function()
        print("🔴 Close button clicked")
        Tween(MainBg, {Size = UDim2.new(0, 0, 0, 0)}, 0.3)
        wait(0.3)
        ScreenGui:Destroy()
        print("✅ GUI Destroyed")
    end)
    
    -- Minimize Button
    local MinimizeButton = Instance.new("TextButton")
    MinimizeButton.BackgroundTransparency = 1
    MinimizeButton.Position = UDim2.new(0.849, 0, 0.117, 0)
    MinimizeButton.Size = UDim2.new(0, 50, 0, 43)
    MinimizeButton.Font = Enum.Font.GothamBold
    MinimizeButton.Text = "-"
    MinimizeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
    MinimizeButton.TextSize = 24
    MinimizeButton.BorderSizePixel = 0
    MinimizeButton.Parent = TopBar
    
    local isMinimized = false
    MinimizeButton.MouseButton1Click:Connect(function()
        isMinimized = not isMinimized
        print("📦 Minimize toggled:", isMinimized)
        if isMinimized then
            Tween(MainBg, {Size = UDim2.new(0, 806, 0, 62)}, 0.3)
            MinimizeButton.Text = "+"
        else
            Tween(MainBg, {Size = config.Size}, 0.3)
            MinimizeButton.Text = "-"
        end
    end)
    
    -- Top Bar Divider
    local TopDivider = Instance.new("Frame")
    TopDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    TopDivider.Position = UDim2.new(0, 0, 0.14, 0)
    TopDivider.Size = UDim2.new(1, 0, 0, 1)
    TopDivider.BorderSizePixel = 0
    TopDivider.Parent = MainBg
    
    -- Tab Section Background (Scrollable)
    local TabSection = Instance.new("ScrollingFrame")
    TabSection.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
    TabSection.Position = UDim2.new(0, 0, 0.142, 0)
    TabSection.Size = UDim2.new(0, 120, 0, 325) -- Made smaller
    TabSection.BorderSizePixel = 0
    TabSection.ScrollBarThickness = 3
    TabSection.ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255)
    TabSection.CanvasSize = UDim2.new(0, 0, 0, 0)
    TabSection.Parent = MainBg
    
    -- Add UIListLayout for tabs
    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.Padding = UDim.new(0, 4)
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Parent = TabSection
    
    -- Update canvas size when tabs are added
    TabListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
        TabSection.CanvasSize = UDim2.new(0, 0, 0, TabListLayout.AbsoluteContentSize.Y + 10)
    end)
    
    -- Tab Divider
    local TabDivider = Instance.new("Frame")
    TabDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    TabDivider.Position = UDim2.new(0.24, 0, 0.142, 0) -- Adjusted for smaller tab section
    TabDivider.Size = UDim2.new(0, 2, 0, 326)
    TabDivider.BorderSizePixel = 0
    TabDivider.Parent = MainBg
    
    -- Main Content Area
    local ContentArea = Instance.new("Frame")
    ContentArea.Name = "ContentArea"
    ContentArea.BackgroundColor3 = Color3.fromRGB(39, 39, 39)
    ContentArea.Position = UDim2.new(0.27, 0, 0.156, 0) -- Adjusted position
    ContentArea.Size = UDim2.new(0.71, 0, 0, 311) -- Adjusted size
    ContentArea.ClipsDescendants = true
    ContentArea.BorderSizePixel = 0
    ContentArea.Parent = MainBg
    
    local ContentCorner = Instance.new("UICorner")
    ContentCorner.CornerRadius = UDim.new(0, 24)
    ContentCorner.Parent = ContentArea
    
    print("✅ Content area created")
    
    -- Bottom Bar
    local BottomBar = Instance.new("Frame")
    BottomBar.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    BottomBar.Position = UDim2.new(0, 0, 0.87, 0)
    BottomBar.Size = UDim2.new(1, 0, 0, 58)
    BottomBar.BorderSizePixel = 0
    BottomBar.Parent = MainBg
    
    local BottomCorner = Instance.new("UICorner")
    BottomCorner.CornerRadius = UDim.new(0, 22)
    BottomCorner.Parent = BottomBar
    
    -- Bottom Divider
    local BottomDivider = Instance.new("Frame")
    BottomDivider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    BottomDivider.Position = UDim2.new(0, 0, 0.867, 0)
    BottomDivider.Size = UDim2.new(1, 0, 0, 1)
    BottomDivider.BorderSizePixel = 0
    BottomDivider.Parent = MainBg
    
    -- User Profile Image
    local UserProfile = Instance.new("ImageLabel")
    UserProfile.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    UserProfile.BackgroundTransparency = 0
    UserProfile.Position = UDim2.new(0.031, 0, 0.12, 0)
    UserProfile.Size = UDim2.new(0, 69, 0, 44)
    UserProfile.BorderSizePixel = 0
    UserProfile.Image = "https://www.roblox.com/headshot-thumbnail/image?userId=" .. LocalPlayer.UserId .. "&width=150&height=150&format=png"
    UserProfile.Parent = BottomBar
    
    local ProfileCorner = Instance.new("UICorner")
    ProfileCorner.CornerRadius = UDim.new(1, 0)
    ProfileCorner.Parent = UserProfile
    
    -- Username
    local Username = Instance.new("TextLabel")
    Username.BackgroundTransparency = 1
    Username.Position = UDim2.new(0.134, 0, 0.12, 0)
    Username.Size = UDim2.new(0, 115, 0, 37)
    Username.Font = Enum.Font.GothamBold
    Username.Text = "@" .. LocalPlayer.Name
    Username.TextColor3 = Color3.fromRGB(255, 255, 255)
    Username.TextSize = 14
    Username.TextScaled = true
    Username.Parent = BottomBar
    
    -- FPS Counter
    local FpsLabel = Instance.new("TextLabel")
    FpsLabel.BackgroundTransparency = 1
    FpsLabel.Position = UDim2.new(0.542, 0, 0.07, 0)
    FpsLabel.Size = UDim2.new(0, 200, 0, 50)
    FpsLabel.Font = Enum.Font.GothamBold
    FpsLabel.Text = "FPS: 60"
    FpsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    FpsLabel.TextSize = 16
    FpsLabel.Parent = BottomBar
    
    -- Time Label
    local TimeLabel = Instance.new("TextLabel")
    TimeLabel.BackgroundTransparency = 1
    TimeLabel.Position = UDim2.new(0.284, 0, 0.07, 0)
    TimeLabel.Size = UDim2.new(0, 200, 0, 50)
    TimeLabel.Font = Enum.Font.GothamBold
    TimeLabel.Text = "Time: 00:00"
    TimeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TimeLabel.TextSize = 16
    TimeLabel.Parent = BottomBar
    
    print("✅ Bottom bar created")
    
    -- Update FPS and Time
    local lastUpdate = tick()
    local fps = 60
    
    RunService.RenderStepped:Connect(function()
        local now = tick()
        fps = math.floor(1 / (now - lastUpdate))
        lastUpdate = now
        
        FpsLabel.Text = "FPS: " .. fps
        TimeLabel.Text = "Time: " .. os.date("%H:%M:%S")
    end)
    
    -- Make Draggable
    MakeDraggable(MainBg, TopBar)
    
    -- Window Functions
    function Window:AddTab(name)
        print("📑 Adding tab:", name)
        
        local Tab = {}
        Tab.Name = name
        Tab.Elements = {}
        Tab.Button = nil
        Tab.Content = nil
        
        -- Create Tab Button
        local TabButton = Instance.new("TextButton")
        TabButton.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        TabButton.Size = UDim2.new(1, -5, 0, 32) -- Smaller tab buttons
        TabButton.Font = Enum.Font.GothamBold
        TabButton.Text = name
        TabButton.TextColor3 = Color3.fromRGB(255, 255, 255)
        TabButton.TextSize = 12 -- Smaller text
        TabButton.BorderSizePixel = 0
        TabButton.Parent = TabSection
        
        local TabCorner = Instance.new("UICorner")
        TabCorner.CornerRadius = UDim.new(0, 88)
        TabCorner.Parent = TabButton
        
        -- Create Tab Content
        local TabContent = Instance.new("ScrollingFrame")
        TabContent.Name = name .. "Content"
        TabContent.BackgroundTransparency = 1
        TabContent.Size = UDim2.new(1, 0, 1, 0)
        TabContent.ScrollBarThickness = 4
        TabContent.ScrollBarImageColor3 = Color3.fromRGB(255, 255, 255)
        TabContent.CanvasSize = UDim2.new(0, 0, 0, 0)
        TabContent.Visible = false
        TabContent.BorderSizePixel = 0
        TabContent.Parent = ContentArea
        
        local ListLayout = Instance.new("UIListLayout")
        ListLayout.Padding = UDim.new(0, 5)
        ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
        ListLayout.Parent = TabContent
        
        local Padding = Instance.new("UIPadding")
        Padding.PaddingLeft = UDim.new(0, 5)
        Padding.PaddingRight = UDim.new(0, 5)
        Padding.PaddingTop = UDim.new(0, 5)
        Padding.Parent = TabContent
        
        ListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
            TabContent.CanvasSize = UDim2.new(0, 0, 0, ListLayout.AbsoluteContentSize.Y + 10)
        end)
        
        Tab.Button = TabButton
        Tab.Content = TabContent
        
        -- Tab Click
        TabButton.MouseButton1Click:Connect(function()
            print("🖱️ Tab clicked:", name)
            Window:SelectTab(Tab)
        end)
        
        -- Tab Functions
        function Tab:AddButton(config)
            config = config or {}
            config.Name = config.Name or "Button"
            config.Callback = config.Callback or function() end
            
            print("🔘 Adding button:", config.Name)
            
            local ButtonFrame = Instance.new("Frame")
            ButtonFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
            ButtonFrame.Size = UDim2.new(1, -10, 0, 33)
            ButtonFrame.BorderSizePixel = 0
            ButtonFrame.Parent = TabContent
            
            local ButtonName = Instance.new("TextLabel")
            ButtonName.BackgroundTransparency = 1
            ButtonName.Position = UDim2.new(0, 10, 0, 0)
            ButtonName.Size = UDim2.new(0, 154, 1, 0)
            ButtonName.Font = Enum.Font.GothamBold
            ButtonName.Text = config.Name
            ButtonName.TextColor3 = Color3.fromRGB(255, 255, 255)
            ButtonName.TextSize = 14
            ButtonName.TextXAlignment = Enum.TextXAlignment.Left
            ButtonName.Parent = ButtonFrame
            
            local ClickArea = Instance.new("TextButton")
            ClickArea.BackgroundTransparency = 1
            ClickArea.Position = UDim2.new(0.254, 0, 0, 0)
            ClickArea.Size = UDim2.new(0.746, 0, 1, 0)
            ClickArea.Text = ""
            ClickArea.BorderSizePixel = 0
            ClickArea.Parent = ButtonFrame
            
            ClickArea.MouseButton1Click:Connect(function()
                print("✅ Button clicked:", config.Name)
                Tween(ButtonFrame, {BackgroundColor3 = Color3.fromRGB(50, 50, 50)}, 0.1)
                wait(0.1)
                Tween(ButtonFrame, {BackgroundColor3 = Color3.fromRGB(18, 18, 18)}, 0.1)
                config.Callback()
            end)
            
            return ButtonFrame
        end
        
        function Tab:AddToggle(config)
            config = config or {}
            config.Name = config.Name or "Toggle"
            config.Default = config.Default or false
            config.Callback = config.Callback or function() end
            
            print("🔄 Adding toggle:", config.Name)
            
            local ToggleFrame = Instance.new("Frame")
            ToggleFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
            ToggleFrame.Size = UDim2.new(1, -10, 0, 33)
            ToggleFrame.BorderSizePixel = 0
            ToggleFrame.Parent = TabContent
            
            local ToggleName = Instance.new("TextLabel")
            ToggleName.BackgroundTransparency = 1
            ToggleName.Position = UDim2.new(0, 10, 0, 0)
            ToggleName.Size = UDim2.new(0, 154, 1, 0)
            ToggleName.Font = Enum.Font.GothamBold
            ToggleName.Text = config.Name
            ToggleName.TextColor3 = Color3.fromRGB(255, 255, 255)
            ToggleName.TextSize = 14
            ToggleName.TextXAlignment = Enum.TextXAlignment.Left
            ToggleName.Parent = ToggleFrame
            
            local ToggleButton = Instance.new("TextButton")
            ToggleButton.BackgroundColor3 = config.Default and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(39, 39, 39)
            ToggleButton.Position = UDim2.new(0.94, 0, 0.15, 0)
            ToggleButton.Size = UDim2.new(0, 25, 0, 22)
            ToggleButton.Text = ""
            ToggleButton.BorderSizePixel = 0
            ToggleButton.Parent = ToggleFrame
            
            local ToggleCorner = Instance.new("UICorner")
            ToggleCorner.CornerRadius = UDim.new(0, 5)
            ToggleCorner.Parent = ToggleButton
            
            local toggled = config.Default
            
            ToggleButton.MouseButton1Click:Connect(function()
                toggled = not toggled
                print("🔄 Toggle:", config.Name, "=", toggled)
                Tween(ToggleButton, {
                    BackgroundColor3 = toggled and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(39, 39, 39)
                }, 0.2)
                config.Callback(toggled)
            end)
            
            return ToggleFrame
        end
        
        function Tab:AddDivider()
            print("➖ Adding divider")
            
            local Divider = Instance.new("Frame")
            Divider.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            Divider.Size = UDim2.new(1, -10, 0, 2)
            Divider.BorderSizePixel = 0
            Divider.Parent = TabContent
            
            return Divider
        end
        
        function Tab:AddSection(name)
            print("📋 Adding section:", name)
            
            local Section = Instance.new("Frame")
            Section.BackgroundTransparency = 1
            Section.Size = UDim2.new(1, -10, 0, 33)
            Section.Parent = TabContent
            
            local SectionName = Instance.new("TextLabel")
            SectionName.BackgroundTransparency = 1
            SectionName.Position = UDim2.new(0, 10, 0, 0)
            SectionName.Size = UDim2.new(0, 270, 1, 0)
            SectionName.Font = Enum.Font.GothamBold
            SectionName.Text = name or "Section"
            SectionName.TextColor3 = Color3.fromRGB(255, 255, 255)
            SectionName.TextSize = 16
            SectionName.TextXAlignment = Enum.TextXAlignment.Left
            SectionName.Parent = Section
            
            return Section
        end
        
        function Tab:AddSlider(config)
            config = config or {}
            config.Name = config.Name or "Slider"
            config.Min = config.Min or 0
            config.Max = config.Max or 100
            config.Default = config.Default or 50
            config.Callback = config.Callback or function() end
            
            print("🎚️ Adding slider:", config.Name)
            
            local SliderFrame = Instance.new("Frame")
            SliderFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 18)
            SliderFrame.Size = UDim2.new(1, -10, 0, 50)
            SliderFrame.BorderSizePixel = 0
            SliderFrame.Parent = TabContent
            
            local SliderName = Instance.new("TextLabel")
            SliderName.BackgroundTransparency = 1
            SliderName.Position = UDim2.new(0, 10, 0, 5)
            SliderName.Size = UDim2.new(0, 154, 0, 20)
            SliderName.Font = Enum.Font.GothamBold
            SliderName.Text = config.Name
            SliderName.TextColor3 = Color3.fromRGB(255, 255, 255)
            SliderName.TextSize = 14
            SliderName.TextXAlignment = Enum.TextXAlignment.Left
            SliderName.Parent = SliderFrame
            
            local ValueLabel = Instance.new("TextLabel")
            ValueLabel.BackgroundTransparency = 1
            ValueLabel.Position = UDim2.new(0.7, 0, 0, 5)
            ValueLabel.Size = UDim2.new(0.25, 0, 0, 20)
            ValueLabel.Font = Enum.Font.GothamBold
            ValueLabel.Text = tostring(config.Default)
            ValueLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            ValueLabel.TextSize = 14
            ValueLabel.TextXAlignment = Enum.TextXAlignment.Right
            ValueLabel.Parent = SliderFrame
            
            -- Slider Background
            local SliderBg = Instance.new("Frame")
            SliderBg.BackgroundColor3 = Color3.fromRGB(39, 39, 39)
            SliderBg.Position = UDim2.new(0, 10, 0, 30)
            SliderBg.Size = UDim2.new(1, -20, 0, 10)
            SliderBg.BorderSizePixel = 0
            SliderBg.Parent = SliderFrame
            
            local SliderBgCorner = Instance.new("UICorner")
            SliderBgCorner.CornerRadius = UDim.new(1, 0)
            SliderBgCorner.Parent = SliderBg
            
            -- Slider Fill
            local SliderFill = Instance.new("Frame")
            SliderFill.BackgroundColor3 = Color3.fromRGB(0, 255, 0)
            SliderFill.Size = UDim2.new((config.Default - config.Min) / (config.Max - config.Min), 0, 1, 0)
            SliderFill.BorderSizePixel = 0
            SliderFill.Parent = SliderBg
            
            local SliderFillCorner = Instance.new("UICorner")
            SliderFillCorner.CornerRadius = UDim.new(1, 0)
            SliderFillCorner.Parent = SliderFill
            
            -- Slider Button (Draggable)
            local SliderButton = Instance.new("TextButton")
            SliderButton.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            SliderButton.Position = UDim2.new((config.Default - config.Min) / (config.Max - config.Min), -8, 0.5, -8)
            SliderButton.Size = UDim2.new(0, 16, 0, 16)
            SliderButton.Text = ""
            SliderButton.BorderSizePixel = 0
            SliderButton.ZIndex = 2
            SliderButton.Parent = SliderBg
            
            local ButtonCorner = Instance.new("UICorner")
            ButtonCorner.CornerRadius = UDim.new(1, 0)
            ButtonCorner.Parent = SliderButton
            
            local currentValue = config.Default
            local dragging = false
            
            local function updateSlider(input)
                local pos = math.clamp((input.Position.X - SliderBg.AbsolutePosition.X) / SliderBg.AbsoluteSize.X, 0, 1)
                currentValue = math.floor(config.Min + (config.Max - config.Min) * pos)
                
                ValueLabel.Text = tostring(currentValue)
                Tween(SliderFill, {Size = UDim2.new(pos, 0, 1, 0)}, 0.1)
                Tween(SliderButton, {Position = UDim2.new(pos, -8, 0.5, -8)}, 0.1)
                
                config.Callback(currentValue)
            end
            
            -- Mouse/Touch support
            SliderButton.MouseButton1Down:Connect(function()
                dragging = true
            end)
            
            SliderBg.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true
                    updateSlider(input)
                end
            end)
            
            UserInputService.InputChanged:Connect(function(input)
                if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    updateSlider(input)
                end
            end)
            
            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = false
                end
            end)
            
            return SliderFrame
        end
        
        table.insert(Window.Tabs, Tab)
        print("✅ Tab added successfully:", name)
        return Tab
    end
    
    function Window:SelectTab(tab)
        print("🎯 Selecting tab:", tab.Name)
        
        for _, t in pairs(Window.Tabs) do
            t.Content.Visible = false
            Tween(t.Button, {BackgroundColor3 = Color3.fromRGB(30, 30, 30)}, 0.2)
        end
        
        tab.Content.Visible = true
        Tween(tab.Button, {BackgroundColor3 = Color3.fromRGB(50, 50, 50)}, 0.2)
        Window.CurrentTab = tab
        
        print("✅ Tab selected:", tab.Name)
    end
    
    -- Opening Animation
    MainBg.Size = UDim2.new(0, 0, 0, 0)
    Tween(MainBg, {Size = config.Size}, 0.5, Enum.EasingStyle.Back)
    
    print("🎉 Window created successfully!")
    print("==========================================")
    
    return Window
end

print("✅ Fryzer UI Library loaded successfully!")

return FryzerUI
